{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:frontend": "cd ../frontend-react && npm run build:laravel",
        "serve": "npm run build:frontend && php artisan serve",
        "serve:quick": "php artisan serve"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}